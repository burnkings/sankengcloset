<template>
  <MainLayout>
    <template #navbar>
      <AppNavbar title="我的" :show-back="false" />
    </template>
    <template #default>
      <view :style="sectionWrap">
        <view :style="cardStyle" @tap="goManage">
          <text :style="cardTitle">管理中心</text>
          <text :style="cardDesc">衣橱、订单、提醒、预算</text>
        </view>
        <view :style="cardStyle">
          <text :style="cardTitle">设置</text>
          <text :style="cardDesc">主题、通知、关于</text>
        </view>
      </view>
    </template>
  </MainLayout>
</template>
<script setup lang="uts">
import { brand, neutralLight } from '@/theme/tokens/colors'
import { spacing, semanticSpacing } from '@/theme/tokens/spacing'
import { radius, semanticRadius } from '@/theme/tokens/radius'
import { shadow } from '@/theme/tokens/shadow'
import { fontSize, fontWeight } from '@/theme/tokens/typography'
import { n } from '@/theme/use-theme'
import MainLayout from '@/components/layout/MainLayout.uvue'
import AppNavbar from '@/components/base/AppNavbar.uvue'

const sectionWrap = { padding: semanticSpacing.pageHorizontal, paddingTop: spacing[3], gap: spacing[3] }
const cardStyle = computed((): any => ({
  padding: spacing[4],
  borderRadius: semanticRadius.card,
  backgroundColor: n.value.surface,
  boxShadow: shadow.sm,
}))
const cardTitle = computed((): any => ({ fontSize: fontSize.base, fontWeight: fontWeight.semibold, color: n.value.text }))
const cardDesc = computed((): any => ({ fontSize: fontSize.sm, color: n.value.textSecondary, marginTop: spacing[0.5] }))

function goManage() {
  // Phase 2: navigate to management center
}
</script>
