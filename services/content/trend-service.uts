/**
 * TrendService — DEPRECATED in 12-table schema
 *
 * Price history/trends removed. No sale_events table, no trend aggregation.
 * This file is kept as a stub for backward compatibility with any remaining imports.
 * API /api/v1/trends may return empty data or 404 until fully removed.
 */
import { isLocalMockSession } from '@/config/runtime'

export class TrendTopic {
  id: string = ''
  name: string = ''
  meta: string = ''
  targetType: string = 'topic'
  targetId: string = ''
}

function fallbackTopics(): TrendTopic[] {
  // Empty: no more trend data in 12-table schema
  return []
}

export async function fetchTrendTopics(period: string = '30d'): Promise<TrendTopic[]> {
  // Trends removed in 12-table schema: return empty
  return []
}
