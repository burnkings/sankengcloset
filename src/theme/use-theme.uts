/**
 * useTheme — 主题切换
 */

import { neutralLight, neutralDark } from './tokens/colors'

const isDark = ref(false)

export function useTheme() {
  return {
    isDark,
    neutral: computed(() => isDark.value ? neutralDark : neutralLight),
  }
}
